# Minimal Fictional Example

These files demonstrate the data shape only. They do not represent a real application or business screen.

The component registry contains one fictional component that passed automatic component review and is `ready_for_use`. The screen registry assembles a fictional screen from that quality-gated component. The visual report records a passed screen comparison.

Replace every name, node ID, path, token, device, locale, and entry point before use.
